# Core AgentCore and Strands packages - ALWAYS REQUIRED
bedrock-agentcore>=0.1.0  # Official AWS AgentCore tools (includes code_interpreter_client)
strands-agents>=1.10.0
strands-agents-tools>=0.2.9  # Community tools (includes alternative code_interpreter)
# AWS SDK for Bedrock and other AWS services - ALWAYS REQUIRED
boto3>=1.34.0
botocore>=1.34.0

# Additional dependencies for the expert agent
pydantic>=2.0.0  # For data validation and serialization
typing-extensions>=4.0.0  # For enhanced type hints
pathlib2>=2.3.0  # For enhanced path operations (if needed on older Python)

# Optional dependencies that might be needed based on tools used
requests>=2.31.0  # For HTTP requests if needed by tools
urllib3>=1.26.0  # For URL handling